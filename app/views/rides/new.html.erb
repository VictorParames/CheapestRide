



<h1 class="text-center text-dark font-weight-bold display-3 mb-5 mt-4">Request a Ride</h1>

<div class="container p-4 bg-dark">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-md-10 col-sm-12">
      <div class="card shadow-lg border-0 rounded-4 p-4 bg-light">
        <div class="card-body">
          <%= simple_form_for @ride do |r| %>

            <!-- Pickup Field -->
            <div class="form-group mb-4">
              <%= r.label :pickup, "Pickup Location", class: "form-label" %>
              <%= r.input :pickup,
                input_html: {
                  data: { address_autocomplete_target: "address" },
                  class: "form-control d-none"
                },
                wrapper_html: { data: { controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"] } }
              %>
            </div>

            <!-- Dropoff Field -->
            <div class="form-group mb-4">
              <%= r.label :dropoff, "Dropoff Location", class: "form-label" %>
              <%= r.input :dropoff,
                input_html: {
                  data: { address_autocomplete_target: "address" },
                  class: "form-control d-none"
                },
                wrapper_html: { data: { controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"] } }
              %>
            </div>

            <!-- Submit Button -->
            <div class="text-center mt-4">
              <%= r.submit "Request Ride", class: "btn btn-primary btn-lg px-5 py-3 shadow-lg rounded-pill fw-bold" %>
            </div>

          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<footer class="bg-dark text-white py-3 mt-5">
  <div class="container text-center">
    <!-- Social Links -->
    <div>
      <a href="https://www.facebook.com" class="text-white mx-2" target="_blank" title="Facebook">
        <i class="fab fa-facebook fa-2x"></i>
      </a>
      <a href="https://www.twitter.com" class="text-white mx-2" target="_blank" title="Twitter">
        <i class="fab fa-twitter fa-2x"></i>
      </a>
      <a href="https://www.linkedin.com" class="text-white mx-2" target="_blank" title="LinkedIn">
        <i class="fab fa-linkedin fa-2x"></i>
      </a>
      <a href="https://www.instagram.com" class="text-white mx-2" target="_blank" title="Instagram">
        <i class="fab fa-instagram fa-2x"></i>
      </a>
    </div>

    <!-- Optional Footer Text (e.g., copyright) -->
    <p class="mt-2 mb-0">© 2025 Bora. All rights reserved.</p>
  </div>
</footer>
